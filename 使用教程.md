# 使用教程

本项目基于 Ultralytics RT-DETR，集成了自定义模块（IRA / FDRRTDETRDecoder / BPDD），
训练与验证入口为根目录下的 `train.py` 和 `val.py`。

## 环境准备

```bash
pip install -r requirements.txt
```

数据集配置在 `configs/VisDrone.yaml`，使用前请确认其中的 `path` 指向本机数据集实际位置：

```yaml
path: /path/to/datasets/VisDrone
train: images/train
val: images/val
test: images/test
```

目录结构要求（YOLO 格式）：

```
datasets/VisDrone/
├── images/
│   ├── train/
│   ├── val/
│   └── test/
└── labels/
    ├── train/
    ├── val/
    └── test/
```

## 训练

```bash
python train.py
```

关键参数（在 `train.py` 的 `overrides` 中修改）：

| 参数 | 当前值 | 说明 |
|---|---|---|
| `model` | `configs/rtdetr-l-fdr-bpdd-ira.yaml` | 模型结构配置，`configs/` 下还有消融实验配置（`no-fgl`、`no-prebox` 等） |
| `data` | `configs/VisDrone.yaml` | 数据集配置 |
| `epochs` | 100 | 训练轮数 |
| `batch` | 8 | 批大小，显存不足时调小 |
| `imgsz` | 640 | 输入尺寸 |
| `workers` | 4 | dataloader 线程数，Windows 下卡死可设为 0 |
| `patience` | 0 | 0 表示不早停；设 30 表示精度 30 轮无提升则早停 |
| `pretrained` | False | 从头训练 |
| `optimizer` | MuSGD | 该 trainer 强制要求 MuSGD，请勿修改 |
| `project` / `name` | `runs/train` / `exp` | 输出目录 |

### 指定显卡

在 `train.py` 开头取消注释，或在 overrides 中设置 `device`：

```python
os.environ["CUDA_VISIBLE_DEVICES"] = "0"   # 用第一张卡
# 'device': '0,1',                        # 多卡训练
```

### 断点续训

在 overrides 中设置：

```python
'resume': 'runs/detect/runs/train/exp/weights/last.pt',
```

## 测试 / 验证

```bash
python val.py
```

`val.py` 默认加载训练输出的最优权重，路径在文件内指定：

```python
model_path = 'runs/detect/runs/train/exp/weights/best.pt'
```

如更换了实验名（`name`）或想测试其他权重，直接修改 `model_path` 即可。

常用选项：

- `split='val'`：可选 `train` / `val` / `test`，取决于数据集划分。
- `save_json=True`：取消注释可输出 COCO 格式结果（用于计算 COCO 指标）。
- `batch`、`imgsz`：与训练保持一致即可。

### 输出内容

验证完成后会打印并保存论文用数据：

- 每类的 Precision / Recall / F1 / mAP50 / mAP75 / mAP50-95 及整体平均；
- GFLOPs、参数量、前处理/推理/后处理耗时、FPS、模型文件大小。

结果保存至 `runs/detect/runs/val/exp/paper_data.txt`。

## runs 目录说明

由于 Ultralytics 会在 `runs/detect` 下再拼接 `project/name`，实际输出路径为嵌套结构：

```
runs/detect/runs/
├── train/exp/weights/
│   ├── best.pt      # 最优权重（val.py 默认读取）
│   └── last.pt      # 最后一轮权重（用于断点续训）
└── val/exp/
    ├── paper_data.txt   # 论文用指标汇总
    └── ...              # PR 曲线、混淆矩阵等图表
```

## 常见问题

1. **Windows 下 dataloader 卡住**：将 `workers` 设为 0。
2. **显存不足（OOM）**：减小 `batch`，或减小 `imgsz`。
3. **加载权重报模块找不到**：`val.py` 已负责注册自定义模块，请勿删除文件开头的
   `register_fdr_module()` 与 `FDRBPDDIRADetectionModel` 导入。
4. **修改实验名后找不到权重**：同步修改 `val.py` 中的 `model_path`。
