diff --git a/ultralytics/engine/validator.py b/ultralytics/engine/validator.py
--- a/ultralytics/engine/validator.py
+++ b/ultralytics/engine/validator.py
@@ -158,7 +158,8 @@ class BaseValidator:
             self.device = trainer.device
             self.data = trainer.data
             # Keep training validation read-only: inputs may be fp16, but EMA/model weights stay fp32 under autocast.
-            self.args.quantize = 16 if (self.device.type != "cpu" and trainer.amp) else None
+            # RT-DETR validation loss is numerically unstable under AMP; both paired arms validate in FP32.
+            self.args.quantize = None
             model = trainer.ema.ema or trainer.model
             if trainer.args.compile and hasattr(model, "_orig_mod"):
                 model = model._orig_mod  # validate non-compiled original model to avoid issues
