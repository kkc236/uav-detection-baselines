# LRS-GFDR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create an explicit LRS-GFDR module configuration and evidence trail, verify it locally, and run the frozen VisDrone experiment on the configured GPU server.

**Architecture:** Reuse the existing LRS-FDR trainer and decoder. Expose feasible geometry explicitly in a new configuration and module metadata, while retaining the current `alpha=0.25` LRS loss behavior. The new run gets an isolated output directory and is compared only after completion.

**Tech Stack:** Python, PyTorch, Ultralytics RT-DETR, YAML model configs, pytest, SSH.

---

### Task 1: Add explicit LRS-GFDR configuration and method note

**Files:**
- Create: `configs/rtdetr-l-lrs-gfdr.yaml`
- Create: `docs/LRS_GFDR_METHOD_ZH.md`

- [ ] **Step 1: Copy the LRS-FDR graph into the new config and set explicit geometry.**
- [ ] **Step 2: Add a method note describing the forward path and exclusions (BPDD/FIA absent).**
- [ ] **Step 3: Build the model locally and inspect resolved options.**

### Task 2: Add regression coverage

**Files:**
- Create: `tests/test_lrs_gfdr_module.py`

- [ ] **Step 1: Test configuration values and module exclusion contract.**
- [ ] **Step 2: Test feasible decoder outputs are finite and have positive extents on a small synthetic tensor.**
- [ ] **Step 3: Run the focused test file and the existing config/model smoke tests.**

### Task 3: Commit and deploy

**Files:**
- Modify: git history only

- [ ] **Step 1: Commit the configuration, documentation, and tests.**
- [ ] **Step 2: Push the branch to the configured GitHub remote.**
- [ ] **Step 3: Transfer the new config and trainer-compatible source to the server without overwriting prior runs.**

### Task 4: Run and monitor the frozen experiment

**Files:**
- Create remotely: `/root/sj-tmp/runs/visdrone-v2-lrs-gfdr-20260908/formal-seed0-lrs_gfdr-v1/`

- [ ] **Step 1: Start the direct `LRSFDRTrainer` run with the frozen contract and new output root.**
- [ ] **Step 2: Verify process liveness, result row growth, and absence of NaN/invalid-box errors.**
- [ ] **Step 3: After completion, extract best/final metrics and write the evidence report locally.**
