# RT-DETR 无人机小目标检测：全部尝试、实验结果与可参考方法

更新时间：2026-07-31

## 1. 文档目的

本文汇总当前工作区、Git 历史、服务器结果分支和正式会议论文检索中能够核验的内容，回答四个问题：

1. 当前 RT-DETR matched baseline 到底是多少；
2. 已经尝试过哪些网络、训练、Query、多视图和诊断路线；
3. 哪些结果是真正的端到端模型结果，哪些只是短筛、Oracle、冻结缓存或推理路由结果；
4. 下一条成功概率最高、同时仍有机会形成 C 类会议论文的网络模块路线是什么。

文中所有“百分点”均指原始 AP 小数差乘以 100。例如 `0.24170 -> 0.25170` 是 `+1.00 pp`，不是 `+0.01 pp`。

## 2. 统一实验背景

| 项目 | 当前冻结设置 |
|---|---|
| 基础模型 | Ultralytics RT-DETR-L |
| Ultralytics | 8.4.90 |
| 数据集 | VisDrone |
| 输入尺寸 | 640 |
| 初始化 | matched baseline 为 `pretrained=False`，从零训练 |
| 训练轮数 | baseline 100 epoch |
| Query | stock 300 queries |
| 验证 | `max_det=300`，NMS 关闭 |
| 主指标 | COCO-style mAP50-95 |
| 辅助指标 | Precision、Recall、AP50、AP75、AP-small/medium/large、tiny AP/Recall |

需要特别注意：

- 不同历史实验使用过不同数据子集、checkpoint 或 evaluator，绝对数值不能跨表直接比较；
- 只有“同 checkpoint、同数据、同 evaluator、同 max_det”的差值才能进行严格归因；
- 冻结 Query cache、Oracle 和多视图推理结果不能冒充端到端网络训练结果；
- 当前用户要求最终贡献必须是推理时真实存在的网络模块，训练-only loss 和推理后处理不能作为核心贡献。

## 3. Matched baseline

### 3.1 100 epoch 训练记录

来自完整 `results.csv` 的第 100 epoch：

| Epoch | Precision | Recall | AP50 | mAP50-95 |
|---:|---:|---:|---:|---:|
| 100 | 0.51131 | 0.43493 | 0.41451 | 0.24170 |

### 3.2 当前原生验证器复评

同一成熟 baseline 在后续 Ultralytics RT-DETR 原生验证协议下的复评：

| Precision | Recall | AP50 | AP75 | mAP50-95 |
|---:|---:|---:|---:|---:|
| 0.511452 | 0.435000 | 0.414487 | 0.239399 | 0.241803 |

两组结果只存在日志精度和复评协议带来的极小差异。本文进行历史长训比较时使用 `0.24170`；讨论最新原生端到端验证时使用 `0.241803`。

## 4. 证据等级

| 等级 | 含义 | 可以支持什么 |
|---|---|---|
| A | 完整端到端训练，并用真实模型验证 | 可以判断该网络实现是否超过 matched baseline |
| B | 同条件 3/10 epoch 短筛 | 只能判断是否值得长训 |
| C | Oracle、冻结 cache、同 checkpoint on/off、覆盖率审计 | 只能判断机制上限或失效原因 |
| D | 多视图/切片/推理路由 | 可以证明现象有效，但不满足“网络模块”限制 |
| E | 设计完成但未运行 | 不能声称任何指标收益 |

## 5. 已完成的端到端网络长训

### 5.1 总表

| 方法 | 最佳 epoch | Precision | Recall | AP50 | mAP50-95 | 相对 baseline mAP | 结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| Matched RT-DETR-L | 100 | 0.51131 | 0.43493 | 0.41451 | 0.24170 | 0 | 当前主基线 |
| BTD-SE | 66 | 0.44098 | 0.38683 | 0.35068 | 0.19867 | **-4.303 pp** | 失败 |
| IOQC-SA | 100 | 0.51443 | 0.42183 | 0.40575 | 0.23335 | **-0.835 pp** | 最接近 baseline，但仍失败 |
| VSF-RMR | 69 | 0.45061 | 0.40352 | 0.35943 | 0.20374 | **-3.796 pp** | 失败 |
| ACR-EG 原生集成 | 99 | 0.36270 | 0.34210 | 0.27430 | 0.14115 | **-10.066 pp** | 冻结 cache 正信号未转化为真实模型收益 |

### 5.2 BTD-SE

BTD-SE 尝试用背景可靠性、显著性和细节残差增强 UAV 小目标特征。

完整长训结果：

| 对比 | Precision | Recall | AP50 | mAP50-95 |
|---|---:|---:|---:|---:|
| Baseline | 0.51131 | 0.43493 | 0.41451 | 0.24170 |
| BTD-SE best | 0.44098 | 0.38683 | 0.35068 | 0.19867 |
| Delta | -7.033 pp | -4.810 pp | -6.383 pp | -4.303 pp |
| BTD-SE epoch 100 | 0.38645 | 0.30452 | 0.25170 | 0.10785 |

主要诊断：

- 显著性门均值 `0.5043 -> 0.1337`，模型主动压低该路径；
- 背景分支均值 `0.5240 -> 0.7402`；
- `gamma_mean` 从 `-0.00118` 漂移到 `-0.01470`；
- background target 中正值比例为 `88.77%`；
- P3 上 tiny 目标面积小于 4 个 cell 的比例为 `57.97%`；
- P3 上 tiny 目标宽度小于 2 个 cell 的比例为 `65.64%`；
- background residual 的高分位与 target residual 大量重叠，直接增强会同时放大 hard background。

结论：不是单个超参数问题，而是背景/残差增强路径与主检测表示存在结构性竞争。

### 5.3 IOQC-SA

IOQC-SA 尝试用 Query ownership/competition 和尺度对齐约束改善 Query 学习。

| 对比 | Precision | Recall | AP50 | mAP50-95 |
|---|---:|---:|---:|---:|
| Baseline | 0.51131 | 0.43493 | 0.41451 | 0.24170 |
| IOQC-SA | 0.51443 | 0.42183 | 0.40575 | 0.23335 |
| Delta | **+0.312 pp** | **-1.310 pp** | **-0.876 pp** | **-0.835 pp** |

它表现为 Precision 略升、Recall 和 AP 下降。说明抑制竞争 Query 容易同时压掉仍有价值的候选。该路线也与 EASE-DETR、DDQ、Group DETR、H-DETR、Co-DETR 等成熟 Query competition 工作高度拥挤。

### 5.4 VSF-RMR

VSF-RMR 尝试通过可见尺度特征与关系建模改善小目标表示。

| 对比 | Precision | Recall | AP50 | mAP50-95 |
|---|---:|---:|---:|---:|
| Baseline | 0.51131 | 0.43493 | 0.41451 | 0.24170 |
| VSF-RMR | 0.45061 | 0.40352 | 0.35943 | 0.20374 |
| Delta | -6.070 pp | -3.141 pp | -5.508 pp | -3.796 pp |

结论：强耦合的多尺度特征改造没有在当前训练协议下形成稳定收益。

### 5.5 ACR-EG / GCQF 原生集成

ACR-EG 是 GCQF 的第三阶段，尝试把全图和四个局部视图的 Decoder Query 通过几何投影、全局-局部交互、tiny utility、non-tiny risk 和有界准入残差融合进真实 RT-DETR。

真实 Ultralytics 原生端到端验证：

| 方法 | Precision | Recall | AP50 | AP75 | mAP50-95 |
|---|---:|---:|---:|---:|---:|
| Baseline | 0.511452 | 0.435000 | 0.414487 | 0.239399 | 0.241803 |
| ACR-EG | 0.362703 | 0.342102 | 0.274295 | 0.127001 | 0.141146 |
| Delta | -14.875 pp | -9.290 pp | -14.019 pp | -11.240 pp | **-10.066 pp** |

结论：冻结 Query cache 上的正结果没有转化为真实端到端网络结果；不能再用 cache 结果宣称模块有效。

## 6. BTD-SE、P2、IOQC 与 NWD 短筛

### 6.1 同条件 stock control

| 实验 | Epoch/Fraction | Best mAP50-95 |
|---|---:|---:|
| Stock control | 3ep/3% | 0.04142 |
| Stock control | 10ep/10% | 0.09144 |

### 6.2 BTD-SE / residual / P2 全部短筛

| 实验 | Epoch/Fraction | Best | 相对 stock | 结论 |
|---|---:|---:|---:|---|
| v3 aux no-rank | 3ep/3% | 0.04230 | +0.00088 | 早期微正 |
| v3 aux no-rank | 10ep/10% | 0.09055 | -0.00089 | 失败 |
| v3 no-aux | 10ep/10% | 0.08771 | -0.00373 | 失败 |
| v3 bg=0.05/sal=0.1 | 10ep/10% | 0.08895 | -0.00249 | 失败 |
| v3 bg=0.03/sal=0.1 | 10ep/10% | 0.08514 | -0.00630 | 失败 |
| v3 bg=0.02/sal=0.1/warm3 | 10ep/10% | 0.08831 | -0.00313 | 失败 |
| v3 enhance warmup5 | 10ep/10% | 0.08702 | -0.00442 | 失败 |
| v3 gamma cap 0.03 | 10ep/10% | 0.08792 | -0.00352 | 失败 |
| v4 detach | 10ep/10% | 0.08647 | -0.00497 | 失败 |
| refine v1 detach | 10ep/10% | 0.08760 | -0.00384 | 失败 |
| P2 fuse v1 | 3ep/3% | 0.03229 | -0.00913 | 明显失败 |
| P2 residual v1 | 3ep/3% | 0.04147 | +0.00005 | 等同噪声 |
| P2 residual v1 | 10ep/10% | 0.08852 | -0.00292 | 失败 |
| P2 residual v2 | 3ep/3% | 0.04033 | -0.00109 | 失败 |

结论：当前项目已经充分证伪“继续微调 P2 加法、concat、residual、background gate、gamma、detach 或 warmup 就会成功”的判断。

### 6.3 IOQC alignment-only

| 实验 | Epoch/Fraction | Best | 相对 stock | 结论 |
|---|---:|---:|---:|---|
| align=0.005, competition=0 | 3ep/3% | 0.04219 | +0.00077 | 早期微正 |
| align=0.005, competition=0 | 10ep/10% | 0.09064 | -0.00080 | 未通过 |

### 6.4 NWD

| 实验 | Epoch/Fraction | Best | 相对 3ep stock | 结论 |
|---|---:|---:|---:|---|
| NWD auxiliary lambda=0.005 | 3ep/3% | 0.04132 | -0.00010 | 未通过 |
| NWD auxiliary lambda=0.002 | 3ep/3% | 0.04068 | -0.00074 | 失败 |
| NWD matcher cost=2 + auxiliary lambda=0.002 | 3ep/3% | 0.03986 | -0.00156 | 失败 |

NWD 作为 tiny-object localization 度量是成熟方向，但当前 matcher/loss 接法没有产生收益，而且它主要是训练目标，不符合“推理时存在的网络模块”要求。

## 7. Query 与候选生成路线

### 7.1 直接 P2 Query injection

| 指标 | 结果 |
|---|---:|
| 新增有效候选 `N_gain` | 707 |
| 被挤掉的有效 stock 候选 `N_loss` | 938 |
| 净替换价值 `V_replace` | **-231** |
| 末三轮 mAP50-95 均值差 | `-1.866667e-4` |

结论：问题不是 Query 数量不足，而是低质量 P2 Query 挤掉了更有效的 stock Query。

### 7.2 QG-P2

| 指标 | 结果 |
|---|---:|
| 净替换价值 | +2 |
| AP-tiny | 0 |
| Recall-tiny | 0 |
| quality-IoU Spearman | 约 -0.114 |
| 未匹配局部 tiny GT 的注入 Query | 约 99.6% |

质量门只能重新排序已有候选，不能修复候选来源本身。

### 7.3 No-injection / TSGR-P2

TSGR-P2 尝试只在训练期利用 P2 tiny 监督和梯度路由，推理时完全删除 P2 分支并保持 stock 300 Query。

已完成的只是数值稳定性和隔离验证：

- Query 始终为 `P2=0, stock=300`；
- 固定 AMP scale 128 的探索运行无 skip、NaN 和 Query 违规；
- 探索性 control final mAP 约 `0.00004`，TSGR 约 `0.00008`，量级不足以证明收益；
- 三 seed comparator、30 epoch 累积收益和正式 `E1_PASS/E1_FAIL` 未完成；
- no-injection 的早期微正还受到全局 gradient clipping 污染，不能作为有效证据。

由于该分支推理时被删除，它也不能作为当前要求下的核心网络模块。

### 7.4 BQP：Top-300 外候选 Oracle

BQP 对固定候选清单进行审计：

| 指标 | 数值 |
|---|---:|
| tiny GT 总数 | 30,245 |
| 已覆盖 tiny | 24,240 |
| 漏检 tiny | 6,005 |
| 可形成安全替换对 | 446 |
| 对 6,005 个漏检的覆盖率 | **7.427%** |
| Oracle gain/loss/net | `446 / 0 / +446` |
| 预注册最低覆盖率 | 30% |
| Gate | `G0_FAIL` |

因此，“从固定 rank 301–420 中替换少量尾部 Query”有正 Oracle，但作用范围只有 7.43%，不适合作为主论文方法。

### 7.5 CSHC：Top-300 前高分辨率候选

CSHC 尝试在 Query 选择前从 C2 生成高分辨率候选。

| 指标 | 数值 |
|---|---:|
| 新 C2 candidates | 331,264 |
| 冻结漏检 tiny | 6,005 |
| IoU 0.50 class-aware 覆盖 | 5 |
| 覆盖率 | **0.0833%** |
| 决策 | `NO_GO` |

这直接说明当前高分辨率候选头没有生成可用框。继续增加 Query 数量不会修复候选质量。

### 7.6 “固定增加 20 个 Query”

这是讨论过但尚未运行的方案，证据等级为 E：

- 没有训练结果；
- 额外 Query 会进入 self-attention、Hungarian matching 和最终排序；
- 可能增加重复 FP，也可能干扰原 300 Query；
- CSHC 与 P2 injection 已说明候选质量比 Query 数量更关键；
- 因此不能承诺增加 20 Query 会提高 AP。

## 8. 多视图、切片、SADED 与网络化尝试

### 8.1 SBR 多视图可行性

同一 detector 的全图 Arm A 与全图+局部 Arm C：

| 指标 | Arm A | Arm C | Delta |
|---|---:|---:|---:|
| AP-tiny-SBR | 0.071057 | 0.111092 | +4.0035 pp |
| mAP50-95 | 0.180621 | 0.223033 | +4.2412 pp |
| Tiny recall | 0.553748 | 0.653940 | +10.0192 pp |
| AP75 | 0.166666 | 0.209918 | +4.3252 pp |
| AP-large-SBR | 0.145847 | 0.114053 | **-3.1794 pp** |

它证明局部高分辨率视图确实包含有效 tiny 证据，但这是多视图推理，不是单次前向网络模块。

### 8.2 SADED-SM 历史结果

早期 matched checkpoint 上的单模型五视图路由：

| 指标 | Arm A | SADED-SM | Delta |
|---|---:|---:|---:|
| AP-tiny-SBR | 7.1057% | 11.0251% | +3.9194 pp |
| mAP50-95 | 18.0621% | 20.6470% | +2.5849 pp |
| Tiny recall | 55.3748% | 65.5526% | +10.1778 pp |
| AP75 | 16.6666% | 18.6902% | +2.0237 pp |
| AP-large-SBR | 14.5847% | 14.3938% | -0.1909 pp |

该结果后来被 fresh100 权威端点取代，不能与新端点混用。

### 8.3 SADED-SM fresh100 权威结果

| 指标 | Arm A | SADED-SM | Delta |
|---|---:|---:|---:|
| mAP50-95 | 7.2566% | 8.7968% | +1.5402 pp |
| AP50 | 16.6685% | 21.4744% | +4.8059 pp |
| AP75 | 5.7509% | 6.3757% | +0.6248 pp |
| AP-tiny | 0.9812% | 2.8491% | +1.8680 pp |
| AP-small | 5.9173% | 4.9517% | -0.9656 pp |
| AP-medium | 16.9371% | 16.0876% | -0.8495 pp |
| AP-large | 14.6393% | 14.5965% | -0.0428 pp |
| Tiny recall | 22.2517% | 38.9737% | +16.7220 pp |

结论：这是项目中最明确的正现象，但其核心是五次前向加固定路由，当前不能作为“网络层架构创新”主线。

### 8.4 T-ASCV

T-ASCV 是学习型 tiny-only asymmetric cross-view specialist。它通过了工程 preflight 和 mechanism smoke，但权威 screen 决策为 `TASCV_STOP`，没有进入 100 epoch。现有公开交接没有提供足以支持性能声明的最终数值，因此这里只记录 STOP，不补造数据。

### 8.5 PLEC / GCMV-EI P3 特征注入

PLEC 本身完成了网络结构、梯度、AMP 和参数量验证：

- 参数量 122,497；
- 局部测试 54 passed；
- 全仓回归 909 passed、1 skipped；
- 但单模块验证不包含检测 AP。

集成 PLEC + GGLF + PEG 后的同 checkpoint 诊断：

| 对比 | 原始差值 | 换算为百分点 |
|---|---:|---:|
| Method-On − Method-Off AP-tiny | -0.000002 | -0.0002 pp |
| Method-On − Method-Off mAP50-95 | +0.000014 | +0.0014 pp |
| Method-Off − Control AP-large | -0.020910 | -2.0910 pp |

模块直接作用近乎为零，辅助训练却造成 detector 漂移，因此停止 P3 特征注入。

### 8.6 旧 GCQF / SR-PEG 冻结 cache

相对 Global RT-DETR-L：

| 指标 | Full − Global |
|---|---:|
| mAP50-95 | +1.0595 pp |
| AP-tiny | +1.3412 pp |
| Tiny recall | +5.4338 pp |
| AP-medium | -0.1640 pp |
| AP-large | -0.0096 pp |

但相对内部 Fixed-SADED：

- mAP50-95 `-1.6579 pp`；
- AP-tiny `-3.0630 pp`；
- Fixed 接纳 local 120,326 个，学习门只接纳 23,283 个；
- 学习门导致最终预测少 7,424 个左右，存在严重过度拒绝。

这只是冻结 Query cache 的模块诊断，不是端到端检测结果。

### 8.7 ACR-EG 冻结 cache

修复硬门后的冻结 cache 结果：

| 指标 | Global | Full-GCQF | Delta |
|---|---:|---:|---:|
| mAP50-95 | 0.198699 | 0.208893 | +1.0194 pp |
| AP50 | 0.361808 | 0.389978 | +2.8170 pp |
| AP75 | 0.185303 | 0.189841 | +0.4538 pp |
| AP-tiny | 0.080789 | 0.094000 | +1.3211 pp |
| Tiny recall | 0.582079 | 0.640291 | +5.8212 pp |
| AP-large | 0.154643 | 0.154553 | -0.0090 pp |

但第 5.5 节的真实原生集成结果是 mAP50-95 `0.141146`。因此 cache 的 `+1.0194 pp` 只能作为失败归因证据，不能写入论文主结果。

## 9. SQDA-SGC、geometry gate、SMGT 与 SMOGT

### 9.1 SQDA-SGC 早期阶段

| 阶段 | 训练预算 | 代表 mAP50-95 | 结果 |
|---|---:|---:|---|
| G1 | 3 epoch | final 0.24160，best_fitness 0.24173 | 未通过 |
| G1R | 约 3 epoch | exact mAP 0.24134 | 未通过 |
| G1S | 约 3 epoch | exact mAP 0.24143，best_fitness 0.24177 | 未通过 |
| G2 | 10 epoch | 选中候选约 0.24177 | strict gate 未通过 |

G2 选中候选相对 exact matched baseline：

| 指标 | Delta |
|---|---:|
| mAP50-95 | +0.00310 pp |
| AP50 | +0.00530 pp |
| Precision | **-0.05384 pp** |
| Recall | +0.01479 pp |
| AP-small | +0.00247 pp |
| AP-medium | -0.00606 pp |
| AP-large | -0.06159 pp |

它的 AP 微正，但幅度接近评估噪声，且 Precision 下降。原严格门因此判定 `hold_not_passed`，没有启动 100 epoch。

### 9.2 Geometry trust gate / SMGT

| 阶段 | 训练预算 | 代表结果 | 决策 |
|---|---:|---|---|
| geometry gate G1 | 3 epoch | best_fitness 0.24177 | 仅工程可行 |
| SMGT G1 | 3 epoch | best_fitness 0.24180 | 未严格通过 |
| SMGT G2 | 10 epoch | best_fitness 0.24177 | 未严格通过 |
| SMGT G2R1 | 10 epoch | `selected_checkpoint=null` | 禁止 100 epoch |

SMGT G2R1 的真实 checkpoint 中：

- 总 AP 差值约 `-0.00161 pp` 至 `+0.00608 pp`；
- AP-small 差值约 `-0.00030 pp` 至 `+0.01310 pp`；
- 没有一个 checkpoint 同时满足全部 Precision/Recall/AP 条件。

### 9.3 SMOGT G1R

3 epoch 绝对值：

| Epoch | Precision | Recall | AP50 | mAP50-95 |
|---:|---:|---:|---:|---:|
| 1 | 0.51192 | 0.43583 | 0.41448 | 0.24177 |
| 2 | 0.51124 | 0.43550 | 0.41453 | 0.24176 |
| 3 | 0.51113 | 0.43531 | 0.41443 | 0.24171 |

`selected_checkpoint=null`，只有 `g2_eligible_checkpoint=epoch1.pt`。

### 9.4 SMOGT G2R2

10 epoch 的 mAP50-95 位于 `0.24168–0.24177`，曲线基本横盘。对全部真实更新 checkpoint 的严格复评：

| 指标 | 观察范围 |
|---|---:|
| Δ 总 AP | +0.00256 至 +0.00566 pp |
| Δ AP-small | +0.00843 至 +0.01154 pp |
| Δ max-F1 Precision | 多数为 -0.00442 至 -0.16688 pp；epoch9 为 +0.02732 pp |
| Δ 固定阈值 Precision | 多数为负，最低约 -0.01103 pp |
| Δ 固定阈值 Recall | -0.00258 至 +0.00258 pp |

最终：

```text
selected_checkpoint    = null
g2_eligible_checkpoint = epoch2.pt
training_signal        = false
```

SMOGT 的 AP/AP-small 虽然全部微正，但增益只有千分之几个百分点，不能支撑“多跑几十 epoch 会自然得到 +1 pp”的判断。

## 10. 设计过但没有形成有效实验结果的路线

| 路线 | 当前状态 | 为什么不能写成成果 |
|---|---|---|
| Tiny Query Rescue Head | 设计/Oracle 阶段 | 受 446/6005，即 7.43% 安全替换上限约束 |
| 固定增加 20 Query | 未运行 | 没有候选质量保证，也没有指标 |
| OCAR Query evidence collision redistribution | 设计阶段 | 尚未完成零训练 collision audit 和正式实验 |
| SQDA-P / ABDR | 设计与局部工程阶段 | 没有超过 gate 的端到端结果 |
| SMOGT 正交几何最小修复 | 尚未形成新的独立 G1/G2 正结果 | 当前 G2R2 不能被事后改判 |
| TSGR-P2 | 只完成探索性隔离 | 无三 seed/30ep/100ep 有效性，且推理时删除 |

## 11. 从全部本地结果得到的稳定结论

### 11.1 已被证伪的主线

1. 继续做普通 P2 add/concat/residual；
2. 继续调 BTD-SE 的 gamma、detach、warmup、background/saliency 权重；
3. 直接把低质量 P2 Query 注入固定 300 Query；
4. 仅靠增加 Query 数量解决 tiny 漏检；
5. 对 trailing Query 做更强抑制；
6. 把冻结 cache 或 Oracle 的正增益直接当成网络结果；
7. 把五视图切片路由包装成单次前向网络模块。

### 11.2 仍然成立的事实

1. 成熟 RT-DETR-L baseline 稳定在 mAP50-95 约 `0.2417–0.2418`；
2. 局部高分辨率视图确实包含大量可救回的 tiny 证据；
3. 直接将浅层/局部证据注入主干容易同时放大 hard background；
4. 当前 Query 失败的核心是候选质量，而不是单纯 Query 数量；
5. 当前网络模块中最接近 baseline 的是 IOQC-SA，但仍低 `0.835 pp`；
6. 尚无一个已完成的端到端网络模块超过 matched baseline；
7. 下一条路线应避开 P2/候选/Query 竞争，优先改动成熟且独立的定位回归头。

## 12. 可以参考的正式会议方法

### 12.1 第一优先级：定位分布回归

#### D-FINE，ICLR 2025 Spotlight

- 论文：[D-FINE: Redefine Regression Task in DETRs as Fine-grained Distribution Refinement](https://openreview.net/pdf?id=MFZjrTFE7h)
- 官方代码：[Peterande/D-FINE](https://github.com/Peterande/D-FINE)
- 基础：直接建立在 RT-DETR 上；
- 网络模块：Fine-grained Distribution Refinement（FDR）；
- 训练辅助：Global Optimal Localization Self-Distillation（GO-LSD）；
- Roadmap 中 RT-DETR-HGNetv2-L 为 53.0 AP，加入 FDR 后 53.5 AP，再加入 GO-LSD 后 54.0 AP；
- 另一组纯定位消融中 baseline 53.0、FDR 53.8、GO-LSD 54.5；
- 官方仓库提供配置、权重、日志和 custom dataset fine-tuning。

可借鉴点：不改 Query 来源、不改 Top-300、不注入 P2，只替换 Decoder box regression head。它与当前项目已失败路线的重叠最小，是成功率最高的成熟基座。

注意：直接复现 D-FINE 不是新论文贡献；必须在固定 FDR 基础上提出新的、可消融的尺度/不确定性适配机制。

#### UGS，ICCV 2025

- 论文：[Uncertainty-Aware Gradient Stabilization for Small Object Detection](https://openaccess.thecvf.com/content/ICCV2025/html/Sun_Uncertainty-Aware_Gradient_Stabilization_for_Small_Object_Detection_ICCV_2025_paper.html)
- 核心：把连续回归改为 interval non-uniform 离散分布分类；
- 组件：分类式定位、uncertainty minimization、uncertainty-guided refinement；
- 论文报告在 VisDrone 上将 DINO-5scale 提升 2.6 AP；
- 同时在 anchor-based、anchor-free 和 tiny detector 上验证；
- 本次检索没有定位到可直接复用的官方仓库，工程成熟度低于 D-FINE。

可借鉴点：tiny 目标的 IoU/坐标损失曲率更陡，定位分布的不确定性值得显式建模。它支持“改回归头而不是继续造 Query”的判断。

### 12.2 第二优先级：DETR Query/Encoder 成熟方案

#### Salience-DETR，CVPR 2024

- 论文：[Salience DETR](https://openaccess.thecvf.com/content/CVPR2024/html/Hou_Salience_DETR_Enhancing_Detection_Transformer_with_Hierarchical_Salience_Filtering_Refinement_CVPR_2024_paper.html)
- 官方代码：[xiuqhou/Salience-DETR](https://github.com/xiuqhou/Salience-DETR)
- 机制：scale-independent salience supervision、hierarchical filtering、Query refinement；
- 优点：直接处理两阶段 Query 选择的尺度偏置；
- 风险：改动 Encoder/Query 选择，和本项目 CSHC、SQDA、P2 Query 的失败区域更接近。

#### DQ-DETR，ECCV 2024

- 论文：[DQ-DETR: DETR with Dynamic Query for Tiny Object Detection](https://www.ecva.net/papers/eccv_2024/papers_ECCV/papers/09775.pdf)
- 官方代码：[hoiliu-0801/DQ-DETR](https://github.com/hoiliu-0801/DQ-DETR)
- 机制：categorical counting、counting-guided feature enhancement、dynamic query selection；
- AI-TOD-V2 上 DINO baseline 25.9 AP，DQ-DETR 30.2 AP；
- VisDrone 表中 DINO-DETR 为 35.8 AP，DQ-DETR 为 37.0 AP；
- 风险：动态 Query 数量和密度图已经是成熟拥挤方向，而且当前本地证据显示候选质量而非数量是主要瓶颈。

#### DDQ-DETR，CVPR 2023

- 论文：[Dense Distinct Query for End-to-End Object Detection](https://openaccess.thecvf.com/content/CVPR2023/html/Zhang_Dense_Distinct_Query_for_End-to-End_Object_Detection_CVPR_2023_paper.html)
- 官方代码：[jshilong/DDQ](https://github.com/jshilong/DDQ)
- 机制：先生成 dense queries，再选择 distinct queries；
- COCO 12 epoch、ResNet-50、5-scale 报告 52.1 AP；
- 风险：需要高质量 dense candidates，并带来 Query 去重和更高计算量；本地 CSHC 已显示候选生成质量严重不足。

### 12.3 第三优先级：高分辨率和特征增强

#### QueryDet，CVPR 2022

- 论文：[QueryDet](https://openaccess.thecvf.com/content/CVPR2022/papers/Yang_QueryDet_Cascaded_Sparse_Query_for_Accelerating_High-Resolution_Small_Object_Detection_CVPR_2022_paper.pdf)
- 官方代码：[ChenhongyiYang/QueryDet-PyTorch](https://github.com/ChenhongyiYang/QueryDet-PyTorch)
- COCO 报告总 AP `+1.0`、AP-small `+2.0`，VisDrone 高分辨率推理加速约 2.3 倍；
- 风险：它是 CNN/FPN 高分辨率稀疏检测路线，官方环境较旧；本地 P2/CSHC 已出现直接反证。

#### Universal YOLO Small Object Structure，ACCV 2024

- 论文：[A Universal Structure of YOLO Series Small Object Detection Models](https://openaccess.thecvf.com/content/ACCV2024/html/Hu_A_Universal_Structure_of_YOLO_Series_Small_Object_Detection_Models_ACCV_2024_paper.html)
- 用 P2 替换 P5，并加入 DGB 和 FRM；
- 在 YOLOX、YOLOv7、YOLOv8，VisDrone/TinyPerson 上验证；
- 风险：结构思想适合 CNN detector，但本项目的多组 P2 fuse/residual 已失败，不应再作为最高优先级。

#### SR-TOD，ECCV 2024

- 论文：[Visible and Clear: Finding Tiny Objects in Difference Map](https://www.ecva.net/papers/eccv_2024/papers_ECCV/html/2515_ECCV_2024_paper.php)
- 在 neck 中加入 self-reconstruction head，用输入与重建图的 difference map 引导 DGFE；
- 是真实网络模块；
- 风险：仍属于 feature enhancement，且 difference/high-frequency hard background 可能重现 BTD-SE 的背景放大问题。

#### SET，CVPR 2025

- 论文：[SET: Spectral Enhancement for Tiny Object Detection](https://openaccess.thecvf.com/content/CVPR2025/html/Sun_SET_Spectral_Enhancement_for_Tiny_Object_Detection_CVPR_2025_paper.html)
- HBS 抑制背景高频噪声，API 在训练期用对抗扰动强化关键区域；
- AI-TOD 上相对 RFLA 报告 `+3.2 AP`；
- 风险：HBS 是网络模块，但 API 主要是训练机制；本次检索没有找到成熟官方仓库，且特征增强仍比回归头改造更容易触发本地已观察到的背景问题。

#### Feature Information Driven Position Gaussian Distribution，CVPR 2025

- 论文：[Feature Information Driven Position Gaussian Distribution Estimation for Tiny Object Detection](https://openaccess.thecvf.com/content/CVPR2025/html/Bian_Feature_Information_Driven_Position_Gaussian_Distribution_Estimation_for_Tiny_Object_CVPR_2025_paper.html)
- 使用信息图和多尺度位置高斯分布预测模块增强弱激活 tiny 区域；
- 是 plug-and-play 网络结构；
- 风险：本次检索未找到官方代码，且仍需 P2/多尺度特征注入。

### 12.4 只能作为训练辅助、不能作为核心网络贡献

| 方法 | 会议 | 原因 |
|---|---|---|
| RT-DETRv3 | WACV 2025 | hierarchical dense positive supervision 为训练-only |
| ScaleKD | CVPR 2023 | 主要是教师-学生知识蒸馏 |
| DEIM | 检索到的公开版本以匹配/训练收敛改进为主 | 不能单独满足推理期网络模块限制 |
| NWD/RFLA | ECCV 等 | 主要是距离、label assignment 或 loss |

这些方法可以用于提高训练稳定性，但不能被包装成当前论文唯一的网络层创新。

## 13. 推荐的最高成功率主方案

### 13.1 推荐结论

停止 Query/P2/多视图网络化主线，转向：

> **RT-DETR/D-FINE 上的尺度-不确定性感知分布回归头**

暂定英文名：

> **SU-DB: Scale- and Uncertainty-conditioned Distribution Basis for RT-DETR**

该路线以 D-FINE 的 FDR 为成熟起点，但不照抄其固定分布基：

1. 保留 RT-DETR backbone、hybrid encoder、Top-300 Query selection 和 Decoder attention；
2. 用成熟 FDR 替换直接坐标回归，先确保成熟方案能在 VisDrone 跑通；
3. 构造三个固定、单调的分布投影基：`fine / base / wide`；
4. 每个 Query、每条 box edge 根据 Decoder Query、当前框 `log(w,h)` 和边分布 entropy，预测有界混合残差；
5. 零初始化时严格或数值上等价于标准 D-FINE `base`；
6. tiny/高置信低熵边优先使用更细基，偏差大或不确定边允许更宽修正；
7. 不增加 Query、不生成 P2 候选、不改 Hungarian matching、不做推理后处理。

### 13.2 为什么它比现有路线成功率更高

| 维度 | SU-DB | 当前失败路线 |
|---|---|---|
| 候选覆盖依赖 | 不依赖新增候选 | BQP/CSHC/P2 Query 强依赖 |
| 主干扰动 | 只改 box head | P2/GCMV 改特征主链 |
| Query 竞争 | 不改变 300 Query | Query injection/动态 Query 会改变竞争 |
| 成熟实现 | D-FINE 有官方代码、权重、日志 | 多个本地模块为自研全新实现 |
| 初始化 | 可从 fixed FDR identity 起步 | 多视图/融合模块分布漂移大 |
| 论文故事 | tiny 定位对尺度和不确定性敏感 | 普通 P2/注意力/门控已拥挤 |
| 主要预期收益 | AP75、总 AP、tiny/small localization | 只赌 Recall 或候选覆盖 |

### 13.3 必须先做的成熟复现

第一步不是立即魔改 Ultralytics，而是：

1. 用 D-FINE 官方仓库和官方 D-FINE-L 配置把 VisDrone 转为 COCO 格式；
2. baseline 与 method 使用相同预训练起点，优先使用官方 COCO 或 Objects365 权重；
3. 先得到 RT-DETR-L、fixed FDR、FDR+GO-LSD 三条可复现曲线；
4. 只有 fixed FDR 在 VisDrone 上至少稳定正增益，才实现 SU-DB；
5. 若官方成熟 FDR 在相同协议下仍不升，立即停止该路线，不再进行“魔改救火”。

允许预训练会显著提高“先跑通成熟方案”的成功率，但 baseline 与 method 必须使用相同预训练权重，不能拿预训练 method 对比 scratch baseline。

### 13.4 分阶段止损门

#### Gate 0：成熟复现

- 官方 D-FINE 代码；
- 同数据、同初始化、同训练预算；
- fixed FDR 相对 RT-DETR 总 AP 必须为正；
- 建议最低可继续信号：总 AP `>= +0.3 pp`，或 AP75 `>= +0.5 pp` 且总 AP 不下降。

#### Gate 1：SU-DB 10 epoch matched warm-start

- fixed FDR 与 SU-DB 从同一 checkpoint 开始；
- 只训练回归头/路由器；
- 至少枚举全部真实 checkpoint；
- 主判据为总 AP 和 AP75，不再要求 Precision、Recall、AP-small/medium/large 全部非下降；
- 若最后三轮平均总 AP 不高于 fixed FDR，停止。

#### Gate 2：30 epoch

- 总 AP 相对 fixed FDR 建议至少 `+0.3 pp`；
- AP75 或 tiny/small AP 至少一项形成清晰正增益；
- AP-large 允许小幅波动，但必须完整报告；
- 参数量、GFLOPs 和延迟增幅建议控制在 5% 内。

#### Formal：论文主实验

- VisDrone + AI-TOD-V2 两个数据集；
- RT-DETR-L、D-FINE fixed FDR、SU-DB 三个主对比；
- 至少一个额外规模模型或第二 backbone；
- 报告 AP、AP50、AP75、AP-tiny/AP-small/AP-medium/AP-large、Recall、Params、GFLOPs、延迟；
- 消融：fixed basis、只用 scale、只用 entropy、scale+entropy、edge-shared、edge-specific；
- 机制分析：按目标尺度统计路由到 fine/base/wide 的比例，展示分布熵与定位误差关系；
- 最终模型冻结后再做 test-dev，不能边看 test-dev 边调结构。

## 14. 对“成功率”的诚实边界

没有任何网络模块可以在实验前保证 `80%` 以上概率得到 `+1 pp`。能够提高成功概率的做法是：

1. 使用已经在 RT-DETR 上正式发表并开源的成熟回归头；
2. 先复现，再只改一个明确机制；
3. 使用 identity/near-identity 初始化；
4. 不再触碰已经多次失败的候选生成和 P2 注入；
5. 用 10ep/30ep Gate 及时止损；
6. 论文目标改为“强 baseline 上清晰、可重复的有限增益”，而不是要求所有指标同时上升。

在当前全部证据下，**D-FINE FDR 成熟复现 + SU-DB 小改造**是最值得投入的网络模块路线；UGS 是最重要的第二参考，但由于官方代码成熟度不足，不适合作为第一条工程落地路线。

## 15. 本地与 Git 证据入口

- `docs/UAV_DETECTION_RESEARCH_STATUS.md`
- `docs/INNOVATION1_SCREENING_REPORT_2026-07-22.md`
- `docs/AB_VENUE_50_PAPER_NOVELTY_AUDIT_2026-07-30.md`
- `docs/CCF_C_VISION_20_PAPER_REVERSE_ENGINEERING_2026-07-24.md`
- `EBC_QP_INNOVATION1_HANDOFF_PRIVATE_2026-07-23.md`
- `SQDA_SMOGT_详细交接_2026-07-31.md`
- `SQDA_SMOGT_任务状态_2026-07-31.md`
- GitHub：<https://github.com/kkc236/uav-detection-baselines>

结果状态不得脱离对应 commit、checkpoint、evaluator 和 protocol 单独引用。
